{
  "name": "bell-407-power-check",
  "private": true,
  "version": "1.0.0",
  "description": "Bell 407 power assurance check and engine trend log",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node test/charts.test.mjs",
    "smoke": "node test/smoke.mjs",
    "icons": "node tools/make-icons.mjs",
    "check": "npm run build && npm test && npm run smoke"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "^1.63.0"
  }
}
